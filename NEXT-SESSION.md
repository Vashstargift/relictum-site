# RELICTUM — как продолжить (на 28.08.2026)

Читается вместе с `CLAUDE.md` (паспорт проекта) и `16_product_promos/HOUSE-RULES.md`
(правила текстов, медиа и приёмки кадров). Полная хроника — в памяти Claude:
`project_relictum.md`, затем `project_master_briefing.md` запись **v321** (сессия 26–28.08).
Этот файл — состояние на сегодня и что делать дальше.

---

## 1. Где что лежит

| | |
|---|---|
| Рабочий репозиторий | **`Vashstargift/relictum-site`** (public), локальный клон `~/relictum-vashikmart`, remote `work` |
| Живой сайт | **https://vashstargift.github.io/relictum-site/02_site_v1_gallery/index.html** |
| Исходный репозиторий | `VashikMart/relictum` — прав на запись нет (403), НЕ трогаем |
| Генерация фото | локальный CLI `~/.local/bin/nano-banana` (Gemini). Ключ: `set -a && . ~/.nano-banana/.env && set +a` |
| Генерация видео | только облачная сессия с Higgsfield. Бриф: `16_product_promos/SPIN_VIDEOS.md` |
| Свой домен | **https://relictum.gallery** — работает, SSL до 03.11.2026. Сборка среза и заливка: `DEPLOY.md` |
| Архив Михаила | `~/relictum-mikhail-archive/Реликтум/` — 72 фото + `messages.html` с описаниями |

**Ритуал:** работаем → «сохрани в гит» → «задеплой». Push в `main` = публикация, только по команде владельца.

---

## 2. Состояние коллекции

**Сверено 28.08.2026 загрузкой `catalog.js` и `promo-data.js` — цифры ниже актуальны.**

| Параметр | Значение |
|---|---|
| Экспонатов в каталоге | **111** видимых (в файле 128, 17 скрыты). Миры: Жизнь 49 · Космос 27 · Монументы 20 · Земля 15 |
| Статус | **80 в наличии · 31 «Под заказ»** |
| Промо-записей в `promo-data.js` | 128 (ключ = `id` вида `R–XXXX`, НЕ slug) |
| Интерьерный блок «В вашем доме» | 126 из 128 |
| Блок «При жизни» / «Как нашли» (`life`) | 126 из 128 |
| Видео эпохи (`alive`) | 71 из 128 |
| Галерея (минимум 4 кадра) | 59 из 128 |
| 360°-видео (`spin`) | **35 из 128** — главный пробел |
| Цены | открыты у 92, «по запросу» у 19 |
| Размеры | у 29 стоит «По запросу» |
| Медиатека `shared/img` | **934 файла**, из них 96 mp4 |
| PDF-каталог и прайс-XLSX | собраны на 37 экспонатов — **сильно отстают**, пересобрать |
| Таблица наличия | `09_admin/build_stock_xlsx.py` → Диск `RELICTUM/RELICTUM_наличие.xlsx` (80 позиций / 99 предметов) |

**Свободные артикулы:** ближайший — **R–0633** (31.08 заняты: R–0630 регмаглиптовый Сеймчан,
R–0631/R–0632 сферы из зуба мамонта). Забронированы владельцем и НЕ выдаются:
R–0102, R–0210, R–0625…R–0629.

---

## 3. Что осталось сделать

### Очередь на 31.08.2026 (свежее — сверху)

**Сделано 31.08 (сессия на Маке):**
- ✅ **Логотипы переведены на Inter и обрисованы в кривые.** Подключать Montserrat не имело
  смысла: все три `shared/brand/Relictum_logo_*.svg` используются через `<img>`, а SVG внутри
  `<img>` веб-шрифты не грузит вовсе (и Cormorant там тоже фолбэчил в Didot/Times). Теперь
  текст — контуры (Cormorant Garamond 400 для «RELICTUM», Inter 400 для кириллицы), рендер
  пиксельно одинаков на любой машине. Скрипт обрисовки: `~/relictum-pdf-rebuild-stash/outline_logo.js`
  (fontkit + наши woff2 из shared/fonts).
- ✅ **Лишний пункт «Журнал» убран из шапки** 11 страниц (10 статей + journal.html) —
  шапка как на остальных 25 страницах.
- ✅ **`09_admin/build_stock_xlsx.py` закоммичен** (висел незакоммиченным с 28.08),
  `09_admin/_thumbs/` добавлен в .gitignore.

**⏸ PDF-каталог — НЕ ТРОГАТЬ (команда владельца 31.08).** Пересборка была готова, но
отложена и откачена из рабочей копии. Вся наработка лежит в **`~/relictum-pdf-rebuild-stash/`**:
`catalog_data.js` на 111 экспонатов (31 ручная запись + 80 собраны из catalog.js/promo-data.js,
битых фото нет), `render_catalog_pdf.js` с многостраничным оглавлением, разделом III «Земля»
(дивайдер — situ_amethyst_druse51.jpg; чёрно-студийный кадр минерала доснять в облаке) и
контактами как в подвале сайта (relictum.gallery / +7 495 233 5111 / info@stargift.ru),
свежий `qr_catalog.svg` на https://relictum.gallery, и `merge_catalog_data.js` для повторной
сверки. Когда владелец даст добро — скопировать файлы в `07_product_presentations/` и отрендерить.
Прайс-XLSX тоже не пересобран (та же пауза).

**Можно делать сразу, ни от кого не зависит:**
1. **360° для оставшихся** — spin есть у 35 из 128. ⚠️ Новые обороты делаем ГОРИЗОНТАЛЬНЫМИ
   1172×784: Kling берёт пропорции из старт-кадра, поэтому сперва генерится студийный
   кадр 3:2 и только с него — оборот. Рамным лотам 360° не делаем.
   ⛔ На Маке генерить нечем: Gemini API в биллинг-отказе (429, limit 0 — с 17.08),
   Higgsfield MCP требует авторизации → только облачная сессия.

**Ждёт владельца:**
- Экспликации для R–0622, R–0623, R–0624 — когда появятся, убрать эти id из `NO_EXPLICATION`
  в `09_admin/build_stock_xlsx.py` и пересобрать таблицу.
- Колонки «сертификат квадратный» и «сертификат Минкульта» в таблице наличия заполняет владелец.
- Пресса: ставить ли флагманов (РБК, Коммерсантъ, Ведомости) вверх ленты вместо чистой хронологии.
  Если пришлют ссылку на публикацию The Blueprint — завести двенадцатую карточку.

**Ждёт Михаила:**
- Провенанс и размеры базилозавра (R–0618) и мозазавра (R–0619).
- R–0217 — в прайсе две цены (применена 950 000 ₽, в списке 900 000 ₽), нужно подтверждение.
- «Идентичный клеоницерас в раме» — есть в наличии, карточки нет.

---

**Ждём от Михаила:** размеры 12 экспонатов (в карточке стоит «По запросу»):
R–0103, R–0104, R–0105, R–0219, R–0220, R–0226, R–0228, R–0613, R–0614, R–0615, R–0616, R–0617;
цены и валюту по 6 позициям (черепа пещерного льва № 1 и № 2, пинакозавр,
зубр, волк, череп гиены-динокрокуты); hi-res снимки волка и мамонта; **боковой ракурс черепа
льва № 2 в hi-res** — плюс подтверждение, что это действительно пещерный лев (см. CLAUDE.md).

**Можно делать сразу:**
1. **360°-видео для 42 экспонатов** (сверено 14.08) — в облачной сессии, рецепт и приоритеты
   в `SPIN_VIDEOS.md`. Там же добить `alive` (нет у 43) и интерьеры (нет у 18).
2. **Canva-копия PDF-каталога** — коннектора к Canva нет, импорт файла руками.
3. ~~Оформление у всех экспонатов~~ — сделано 05.08, поле `mount` заполнено у всех 38.
4. ~~Промо-страницы эпох юра и триас~~ — сделаны 05.08, все шесть эпох связаны между собой.
5. **Аммониты R–0224:** цифра «7 предметов» пришла из июльского архива (`экспонаты.json`),
   снят же только крупнейший предмет. Решение владельца 05.08: комплект показан
   **визуализацией подачи** — кадр на полке собран вокруг реального предмета, остальные шесть
   по его образцу, подпись прямо это называет. Когда Михаил пришлёт фото остальных предметов —
   заменить визуализацию настоящей съёмкой и снять пометку.


**⛔ Требует решения владельца (нашли аудитом 05.08):**
- ✅ **РЕШЕНО (20.08): заявки доходят до дома.** Формы шлют POST в `send.php`
  (генерируется билдером, `SEND_PHP` в `build_public_site.py`, лежит на relictum.gallery):
  основной канал — приёмник StarGift `stargift.ru/api/send-form.php` (кладёт в `crm_requests`,
  как лиды stargift.ru), запасной — письмо на `info@stargift.ru`. Обращения протестированы.
  Локальная копия в `localStorage` осталась как черновик/бэкап посетителя.
- **Ящик `hello@relictum.gallery` / `concierge@relictum.gallery` не заведён.** Все кнопки
  «написать» на страницах товаров вели на `hello@relictum.ru` — чужой домен; 05.08 переписаны
  на свой. Пока ящик не создан в панели Бегета, письма уходят в никуда.
- **Телефон дома.** На странице «Дом Relictum» стоял `+7 900 000-00-00` — заглушка на живом
  сайте. Строка убрана (закомментирована рядом), вернуть с настоящим номером.

**✅ Сделано по аудиту 06.08:**
- **Видео перекодированы** (решение владельца: щадящее качество). 53 ролика,
  `ffmpeg -crf 22 -preset slow`: 219 → 57 МБ (−74%). Качество проверено против
  оригиналов из git: PSNR 38–40 дБ, кадры визуально неотличимы. Публичный срез
  296 → 149 МБ. Порог для новых роликов: битрейт выше 4 Мбит/с при 720p — перебор.
- **Девять страниц-визиток больше не дублируют шаблон.** Они удалены из репозитория
  и собираются билдером из `exhibit.html` (`object_pages` / `write_object_pages`),
  тексты для поиска — поле `seo` в `promo-data.js`. Было 3825 строк дублей.
  Эквивалентность доказана посимвольным сравнением текста, ссылок, картинок,
  видео и мета-тегов до и после. **Новый экспонат со своим адресом = добавить
  `seo` в promo-data и `href` в каталог, HTML писать не нужно.**
- Заодно выровнялся порядок блоков: «Биография объекта» стояла ПОСЛЕ кнопки
  заказа у 29 объектов и в середине у 9 — теперь у всех после блока 360°.

**⛔ Требует решения владельца (нашли аудитом 05.08):**
- ✅ **РЕШЕНО (20.08): заявки доходят до дома.** Формы шлют POST в `send.php`
  (генерируется билдером, `SEND_PHP` в `build_public_site.py`, лежит на relictum.gallery):
  основной канал — приёмник StarGift `stargift.ru/api/send-form.php` (кладёт в `crm_requests`,
  как лиды stargift.ru), запасной — письмо на `info@stargift.ru`. Обращения протестированы.
  Локальная копия в `localStorage` осталась как черновик/бэкап посетителя.
- **Ящик `hello@relictum.gallery` / `concierge@relictum.gallery` не заведён.** Все кнопки
  «написать» на страницах товаров вели на `hello@relictum.ru` — чужой домен; 05.08 переписаны
  на свой. Пока ящик не создан в панели Бегета, письма уходят в никуда.
- **Телефон дома.** На странице «Дом Relictum» стоял `+7 900 000-00-00` — заглушка на живом
  сайте. Строка убрана (закомментирована рядом), вернуть с настоящим номером.

**📉 Резервы, найденные аудитом 06.08 (нужно решение владельца):**
- **Видео весят вчетверо больше нужного.** 53 ролика из 69 закодированы с битрейтом
  6–15 Мбит/с при 720p — для фоновых лупов хватает 2–3. Вся библиотека 253 МБ.
  Пробная перекодировка `era_cretaceous.mp4` (ffmpeg, `-crf 22 -preset slow`):
  11,9 → 5,5 МБ (−54%), PSNR 38 дБ — глазом кадры не отличаются; при `-crf 25`
  3,2 МБ (−73%), PSNR 35,4 дБ. Ждёт решения: перекодировать ли и с каким качеством.
  Откат возможен — ролики под git.
- **9 статичных страниц товаров совпадают с шаблоном `exhibit.html` на 96 %**
  (3825 строк вместо ~150). Из-за этого любая правка делается в десяти файлах —
  за одну сессию так уже чинились золотые кнопки, почта на чужом домене и мёртвая
  ветка heroVideo. Предложение: собирать эти страницы билдером из шаблона,
  перенеся их SEO-описания в `promo-data.js` полем `seo`.
- **Мелочи, оставленные намеренно:** у части страниц уровни заголовков идут
  через ступень (h2 → h4) — правка задевает вёрстку; у картинок нет width/height,
  но сдвиг вёрстки и так 0,005 при норме 0,1, так что смысла мало.


---

## 4. Правила, которые уже стоили ошибок

1. **Только реальные экспонаты.** Ничего выдуманного. Владелец правил это дважды.
2. **Фото = img2img с реального снимка.** Меняем окружение, объект пиксельно неизменен.
3. **Себестоимость — никогда наружу.** В публичном Excel только рублёвые цены с сайта.
   ⚠️ В истории git обоих репозиториев остались валютные суммы партнёра — если подтвердится,
   что это закупка, историю надо чистить в обоих.
4. **Деплой только по команде.**
5. **Словарь дома (решения владельца 05.08).**
   - **«Оправа» — запрещённое слово**, везде «оформление». Пункт паспорта — «Оформление».
     Когда опоры нет — не «подача без оправы», а «Оформление по запросу клиента».
   - **Эпоха у метеоритов** — не «Космос», а «До зарождения жизни». «Космос» остаётся
     только миром в фирменной триаде Земля · Жизнь · Космос.
   - **Дата находки** — отдельный пункт паспорта «Дата находки», всегда с годом и «г.»
     («1967 г.»), внутри строки происхождения год не оставлять.
   - **Превью карточки — одна формула для всех:** первая строка «откуда, когда», вторая — размеры.
     Ничего больше: ни «на подставке», ни «музейное качество», ни «подарочный формат»,
     ни «кабинетная рамка», ни «индивидуал». Всё остальное живёт в паспорте и на промо-странице.
     Собирается из полей `location` + `age` + `size`, см. рецепт в истории git (05.08).
   - **Не выдавать генерации за съёмку.** Подписи вроде «каждый ракурс реального экспоната»
     запрещены: фото и 360° у нас сгенерированы. Под 360° стоит «Визуализация: полный оборот
     вокруг объекта». Если кадр собран не с натуры — это должно быть сказано.
   - **«При жизни» только для живого.** У метеоритов и прочего внеземного (`world: cosmos`)
     блок называется «До Земли». Условие зашито в exhibit.html, статичных страницах метеоритов,
     shared/biography.js и генераторе PDF — при добавлении объекта проверить все четыре.
6. **Тексты — люкс-регистр.** Пишет Sonnet-субагент по слотам, главный агент проверяет факты.
   Запрещено: «Войдите в эпоху», «который/которая» в подписях, олицетворения природы
   и времени, «настоящий/уникальный/идеальный/шедевр», кальки (white-glove, natural history,
   «кейс»), точки-разделители « · » (кроме фирменной триады «Земля · Жизнь · Космос»).
7. **Шапка на тёмных страницах (эры, концепты)** — вариант `nav on-dark`: прозрачная,
   светлый текст, подложка НЕ появляется при скролле (решение владельца 05.08). Класс
   `scrolled` там не используется вовсе. Светлая плашка осталась только на светлых страницах.
8. **Чёрные промо-видео — отдельная библиотека.** Восемь роликов музейной подачи лежат
   в `shared/img/promo/` с описью в README рядом. Используются в геройской карусели главной.
   На страницах товаров их нет: первый экран — всегда каноническое фото (решение 05.08).
9. **Единая обвязка страниц.** Шапка и подвал одинаковы на всех 44 страницах. Стили —
   в `shared/chrome.css` (подключается там, где нет `style.css`: эпохи, концепты, паспорт).
   Переменные в нём объявлены НА БЛОКАХ, не в `:root` — иначе перебьют палитру тёмных страниц.
   Кнопки везде 11,5 px / трекинг .22em / паддинг 17×38, эталон в `style.css`.
   Контентный контейнер везде 1120 px.
10. **Скриншоты превью-панели врать умеют** — она морозит анимации и масштабирует кадр.
   Проверять замерами через `javascript_tool`, а не глазами по скриншоту.

11. **Шрифты — свои, с нашего хостинга.** `shared/fonts.css` + `shared/fonts/*.woff2`.
    Раньше 23 страницы тянули Cormorant и Inter с `fonts.googleapis.com`: иностранный сервис
    (против правила дома) и лишний блокирующий запрос из России. Новые страницы подключают
    `<link rel="stylesheet" href="../shared/fonts.css">` — никаких googleapis/gstatic.
12. **Всё кэшируемое версионируется.** `DATA_FILES` в билдере теперь включает и стили.
    Без этого правка `chrome.css` доходила бы до постоянного посетителя только через 7 дней
    (срок кэша в `.htaccess`) — ровно та же ловушка, что уже случилась с `.js`.
13. **В срез уезжает только то, на что есть ссылка.** `prune_media()` в билдере выбрасывает
    медиа, которое нигде не упоминается (библиотека промо-роликов, кадры для PDF, черновые
    генерации) — 35 МБ. В репозитории файлы остаются. `shared/brand/` не трогается никогда.
    Билдер печатает список выброшенного — просматривать его при каждой сборке.

14. **Кнопки в ряду — одно правило на весь сайт, `shared/buttons.css`.**
    Левая кнопка с заливкой, остальные прозрачные с контуром. На тёмном фоне
    заливка белая, на светлом — чёрная; тёмный фон помечается классом `on-dark`
    на контейнере ряда (`btn-row`, `btns` или `row` в блоке покупки).
    Ширины в ряду равные. Две грабли: **никакого `max-width` на ряду** — с ним
    браузер считает каждую колонку по её содержимому и кнопки разъезжаются
    (от переполнения защищает перестроение в столбец: 760px, для рядов из трёх
    кнопок 900px); и **раскладку `.btn-row` задаёт `style.css`**, который
    подключается позже — не дублировать её в buttons.css, там только то, чего в
    style.css нет.
15. **«По запросу», а не «Размеры уточняются»** — формулировка владельца 06.08
    для объектов, у которых размеров пока нет (их 12, список выше).

16. **Фоновые ролики эр — 16:9, не 9:16.** Секции широкие: из вертикального
    ролика на экран попадает только средняя треть, две трети кадра пропадают.
    Широкий кадр проверен и на телефоне — там он тоже читается. Побочно он
    втрое легче: 1920×1080 против 1440×2560 при той же чёткости.
17. **Разрешение: снимать сразу от 1080p.** Старые ролики сняты в 716–720 px и
    растягивались браузером до 1440 — отсюда была мягкая картинка. Сжатие тут
    ни при чём (проверено: 42 дБ против оригинала). Поднять готовый ролик, не
    трогая сцену, умеет `bytedance_video_upscale` (2k, preset aigc, model pro).
    Постер после этого перенарезать из нового ролика.
18. **Брак походки лечится сменой модели, а не промптом.** У неогена животные
    шли задом наперёд; Kling 3.0 Turbo воспроизвёл ту же ошибку даже с прямым
    запретом в промпте. Помогла **Veo 3.1 (quality ultra, variant preview)** —
    сняла 4K с верной биомеханикой. Проверять покадрово с увеличением: на общем
    плане направление силуэта не читается.
19. **Проверять края кадра.** У мамонтов генерация оставила чёрные полосы по 3 px
    слева и справа — при растягивании вдвое они превращались в тёмные линии.

20. **Медиа в разметке версионируется автоматически** — `stamp_media()` в билдере
    дописывает `?v=<хэш содержимого>` ко всем `src/poster/data-src` с картинками
    и видео. Без этого перезаписанный под тем же именем файл месяц не доходит до
    посетителя: в `.htaccess` у медиа кэш 30 дней. Ровно так и вышло с
    переснятыми роликами эр — на сервере уже лежало новое, а в браузере
    оставалось старое. ⚠️ Картинки, которые собирает JS (карточки каталога,
    корзина, кабинет), сюда не попадают — у них по-прежнему ручной `?v=N`.

21. **Страницы эр связаны с каталогом скриптом** `09_admin/build_era_pages.py`.
    Он ставит под каждым существом плашку-ссылку на экспонат того же вида и
    заполняет нижний блок ВСЕМИ предметами эпохи (соответствие эра→`period`
    и существо→id заданы в шапке скрипта: у тираннозавра и трицератопса
    предмета нет, ссылки там не будет). **Прогонять после каждого нового
    экспоната** — иначе страницы эр отстанут от каталога.

---

## 5. Технические уроки этой сессии

- **Правка данных ТОЛЬКО через `re.sub` с функцией.** Замена по индексам в цикле сдвигает офсеты
  и крошит `catalog.js` / `promo-data.js`. Один раз уже испортил оба, спасал `git checkout`.
- **После правки промо-страниц проверять JS:** подвал и разметка там живут внутри JS-строк,
  regex-замена рвёт строку. Проверка: вырезать `<script>` и прогнать `node --check`.
- **Формат фото = формат контейнера.** Карточки 4:5, галереи 3:2. Несовпадение даёт полосы.
- **Доращивать кадр растяжкой краёв НЕЛЬЗЯ** — на фактуре появляются вертикальные полосы.
  Правильно: кадрировать, либо перегенерировать в нужном формате, либо чинить фон
  гладкой моделью (как в правке от 05.08).
- **Замена фото под тем же именем требует cache-buster** — сейчас `?v=11` (58 мест, менять разом).
- **Данные тоже кэшируются.** `catalog.js` и `promo-data.js` Beget отдаёт с `max-age` на неделю,
  поэтому правка каталога у посетителя не появлялась до истечения кэша (05.08 из-за этого
  «зубр всё ещё с черепом»). Сборщик теперь дописывает скриптам `?v=<хэш содержимого>`:
  данные не менялись — адрес прежний, поменялись — адрес новый. Руками ничего делать не нужно.
- **Генерация путает вымершее с современным.** Динокрокута выходила пятнистой гиеной,
  махайрод — львицей без сабель. Лечится подачей нашего же черепа как `--ref`.
- **Генерация умеет задваивать объект** (плита ихтиозавра, рёбра Jeholosaurus).
  Всегда сверять с исходником; исходники доставать из истории git.
- **Опора на фото должна быть физически возможной.** У пинакозавра генерация выдала висящую
  треногу: стойки сквозь лапы, ноги в воздухе. Лечится явным описанием монтажа в промпте —
  «одна сплошная низкая плита из чернёной стали под всем животным, все четыре лапы стоят
  НА плите, две-три тонкие вертикальные стойки, никаких перекрещенных ног и проволоки».
  Проверять глазами каждый кадр: ищем, на чём объект стоит и касается ли он опоры.
- **Смена ракурса = приглашение выдумать.** Череп льва № 2 снят только в анфас; при попытке
  получить профиль модель дважды удлинила морду до медвежьей. Если исходник в одном ракурсе —
  галерею собирать кропами исходника, а интерьер генерить с явным «keep the SAME viewing angle».

---

## 7. Аудит вёрстки (скрипт под рукой)

Обмер всех страниц среза headless-браузером: горизонтальный вылет, ширина контейнеров, высота
шапки, число ссылок в подвале, полосы у картинок (`object-fit:contain` + формат кадра ≠ контейнеру),
начертания кнопок. Прогонять после крупных правок вёрстки.

```bash
cd ~/relictum-vashikmart && python3 09_admin/build_public_site.py
cd public && python3 -m http.server 8132 &
# скрипт аудита: scratchpad/audit.js последней сессии, либо переписать по описанию выше
```

Состояние на 05.08.2026: шапка 88 px и подвал из 13 ссылок на всех 44 страницах, вылета нет
ни на десктопе, ни на 390 px, полос у картинок нет, кнопки одного начертания.

---

## 8. Рабочие команды

```bash
# генерация фото в каноне дома
set -a && . ~/.nano-banana/.env && set +a
~/.local/bin/nano-banana "<промпт из CLAUDE.md>" --ref ph_X.jpg -m pro -a 3:4 -s 2K -o out -d /tmp

# целостность каталога и медиа
node -e "global.window={};require('./shared/catalog.js');require('./16_product_promos/promo-data.js');
const C=window.RELICTUM_CATALOG,P=window.RELICTUM_PROMO,fs=require('fs');let m=[];
for(const k in P){const p=P[k];[p.spin,p.heroVideo,p.interior&&p.interior.img,...(p.gallery||[])]
.filter(Boolean).forEach(f=>{if(!f.startsWith('http')&&!fs.existsSync('shared/img/'+f))m.push(k+':'+f)})}
console.log('объектов:',C.length,'| битые медиа:',m.join(', ')||'нет')"

# пересборка PDF (на Маке)
cd 07_product_presentations && NODE_PATH=/private/tmp/claude-501/-Users-docbrown/*/scratchpad/pwenv/node_modules \
CHROMIUM_PATH="$HOME/Library/Caches/ms-playwright/chromium-1228/chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing" \
node render_catalog_pdf.js

# таблица коллекции (только рубли)
python3 09_admin/build_price_xlsx.py

# публикация
git add -A && git commit -m "…" && git push work main
```
